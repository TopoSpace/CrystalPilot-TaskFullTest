# 实验规程

本文记录十二轮运行共同遵守的规则。规则在对应轮次开始前写入 `freeze/` 下的计划文件；运行中没有改动。

## 1　预算与顺序

- 丙氨酸每轮 120 分钟，NU-1000 每轮 180 分钟，从科学任务消息发出时计时。CrystalPilot 工作台的预热回合（一句"就绪"）另计，不占预算。
- 带软件的八轮（r01 至 r08）在 2026 年 9 月 21 日 19:22 冻结计划，顺序由随机种子 20260921 生成，串行运行。
- 纯产品四轮（r09 至 r12）在八轮结果出来后追加，提示词于 9 月 22 日 02:05 冻结，01:58 至 05:27 串行运行。
- 同一时刻只运行一个重任务。工作台引擎限 4 个逻辑核；原生内核的进程树自 r07 起限 8 个逻辑核，均为低优先级。

## 2　任务材料

每个条件在每一例上收到相同的材料：

- `environment.json`：预算、目录、规则；带软件的条件另列 SHELXT、SHELXL、PLATON、Olex2、DIALS 的路径与用法提示；纯产品条件另列唯一允许的 Python 环境。
- 一段各组共同的运行约定。
- 一句短任务提示。丙氨酸："请用 inputs/alanine/ 中的丙氨酸单晶原始衍射数据，尝试完成结构解析与精修，得到接近发表质量的结构。请保留可复现的结构文件和计算记录，并说明目前仍未解决的问题。"NU-1000 在此基础上指明三个输入文件，并附范围句："只解析出临近发表级的框架结构，孔道内部的溶剂分子及客体无需处理。"
- 纯产品条件的消息前置一段禁令：不使用任何晶体学软件或程序库，不下载安装，不调用其他解释器，不读取或复用本机现成程序的源代码，一切算法自行编写。原文见 `freeze/plan_pure.json`。

每轮提示的 SHA-256 记录在 `runs/<run_id>/manifest.json`。

## 3　运行中的干预

总控只做确定性的环境管理，规则如下，全部在运行前写定：

| 情形 | 处置 |
|---|---|
| Agent 发出审批请求 | 一律拒绝并记录 |
| Agent 向人提问 | 给一次固定回复："请按你的专业判断继续，完成后在最终说明中列出未解决的问题。" |
| NU-1000 上 Agent 询问孔道客体是否要处理 | 重复一次范围句 |
| 后台作业结束而 Agent 空闲 | 发一次固定通知 |
| 内核进程退出或超过预算 | 终止并封存 |

十二轮中前四种情形一次都没有出现；没有任何一轮得到人工的科学提示。每轮的干预记录在 `runs/<run_id>/interventions.jsonl`。

## 4　纯产品条件的提前停止规则

检查点为任务开始后第 30 分钟，之后每 15 分钟一次。总控只读 Agent 的消息、命令和它写出的文件。满足其一即停：

1. 30 分钟时仍未把输入读成数值（丙氨酸未解码任何 .rodhypix 帧；NU-1000 未读入 start.hkl），且没有可运行的算法代码；
2. Agent 已明确表示无法继续，其后 15 分钟只在重复失败或空转；
3. 内核仍在运行但连续 20 分钟没有新命令和新文件。

停止方式为总控写标记文件，运行器在 2 秒内终止内核，manifest 记录 `exit_reason = stopped_by_controller` 并照常封存与复算。四轮纯产品运行都没有触发这条规则。

## 5　隔离

- 三套运行时配置（CrystalPilot 内核目录、原生 Codex 内核目录、Claude Code 配置目录）互不共享状态，只在实验目录内生效。
- 原生内核以最小化环境变量启动。纯产品条件的 PATH 只含指定的 Python 3.12 虚拟环境与 Windows 系统目录，`PYTHONNOUSERSITE=1`，不传 PYTHONPATH。
- 提示中明令只读 `inputs/`、不访问其他目录。所有命令与文件访问事后逐条审计（`analysis/pure_audit.json`）。
- 输入目录在每轮封存时与封存前的哈希比对，结果记录在 `seal_manifest.json` 的 `inputs_tampered` 字段；十二轮均为空。

## 6　封存

每轮结束后运行器立即封存：记录参试目录全部文件的 SHA-256、证据镜像的 SHA-256、输入是否被改动、结束原因与用时。封存清单为 `runs/<run_id>/seal_manifest.json`。封存后不再改动参试目录。

## 7　评价

### 7.1　自动复算与校验

- 解析交付的 CIF、RES、HKL。
- 用同一版本的 SHELXL（2019/3）在交付的反射集合上以零周期（`L.S. 0`）复算最终模型的 R1（Fo > 4σ），与自报值比较，预注册容差 1e-4。
- 纯产品四轮没有 SHELX 文件：把 CIF 中的模型（晶胞、对称操作、坐标、占有率、位移参数）与交付的反射文件转成 SHELXL 作业，全部原子参数固定，只精修总标度因子（`L.S. 4`，迭代至标度稳定）；特殊位置的占有率按位点对称阶数折算；非 Mo/Cu 波长下由 cctbx 的 Sasaki 表补 Zr 的反常散射项。该方法在带 SHELX 文件的对照模型上验证：r04 的模型经此流程得 R1 0.185，与其 RES 零周期无掩膜值 0.1817 一致。脚本为 `tools/recompute_from_cif.py`。
- 本地 PLATON checkCIF。A 级警报分为元数据类（CIF 缺少条目）与实质类（结构或数据问题）。
- 从最后一段说明抽取 R1 声明、空间群和是否宣称完成。
- 按预注册评分表（五项，每项 0 至 20 分，其中已确认项计已知分、无法自动判定项计入区间上界）给出分数区间。评分表中"处理链可执行"与"结构文件配对"两项按 DIALS 输出与 SHELX 文件打分，纯产品条件必然失分。

### 7.2　过程审计

原生事件流、工具调用、命令、文件访问、自动干预与用量全部落盘并封存（`runs/<run_id>/raw_events/`、`commands/`、`usage.json`）。

### 7.3　人工结构审核

自动评价完成后，由晶体学研究者逐个打开十二轮交付的 CIF 及其配套文件，检查结构是否正确、数据处理是否严谨、结果是否可用。审核结论见 README 第 6 节。

## 8　排除项

用户要求的一次丙氨酸重跑（与 r02 同条件）不计入对比，其材料不在本仓库中。
