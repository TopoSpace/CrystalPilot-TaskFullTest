# 非最终结果：首次完整重现尝试

本目录保留第一次完整运行的实际输出。初始全局搜索停在氮原子/甲基碳指认混淆的局部极小值，最终R1约0.0797、wR2约0.2143，**不采用为交付结构**。

此处旧 `validation.json` 的 `all_basic_checks_pass` 仅表示基本数值/文件/几何检查通过，并不表示数值精修质量达标。当前代码已加入数值质量门槛、至少两个全局搜索起点和显式的两种C/N指认比较。

正确且独立重现成功的输出在 `../reproduction_verified/`；推荐最终模型为 `../alanine.cif`。本目录不删除、不覆盖，以保留真实的失败诊断记录。
