# 本次处理记录

## 范围与约束
- 仅使用本次 `inputs/alanine`、`environment.json` 指定的软件及软件通用资料；未读取外部样品结构或查询样品答案。
- inputs 保持只读；所有衍生文件写在本运行目录。
- 运行预算：120 分钟；2026-09-22 约 00:14（本机时间）开始。
- 每次实际程序命令、工作目录、起止时间和退出码写入 `commands.jsonl`；标准输出与错误保存为各步骤 `*.console.log`。

## 输入
- 646 个 `.rodhypix` 文件：正式采集 616 帧（扫描 1–6 分别 92/92/150/58/116/108 帧），预实验 30 帧（`pre_`，3×10）。仅正式扫描用于最终积分；预实验未重复计入。
- 原始帧逐文件 SHA-256 见 `raw_frames_manifest.json`。
- 正式采集记录：2024-02-09；每帧 2 秒；扫描宽度 0.5 度；样品温度实测范围 149.93–150.05 K。`.par` 中 149 K 为另一记录，最终采用实测采集日志约 150 K 并说明来源。
- 帧头给出波长 0.71073 Å；探测器 HyPix Arc100，两面板、100 µm 像素；帧头增益 1。
- 化学式由本次 sample.ini 提供：C3 H7 N O2，Z=4（需结构/晶胞验证）。未从外部结构导入原子坐标。
- 附有采集时晶胞与晶体面列表；晶胞不作为未经检验的最终结果。
- 已直接查看原始照片 `frames/jpg/pgw240033_Mo_diffimg_1.jpg` 和 `movie/pgw240033_Mo1.jpg`；前者为样品照片，后者为衍射图。照片文件名与实际内容不同。照片本身无可直接使用的尺寸标尺；不据此推断晶体尺寸或颜色。

## 导入兼容性
- `dials.import directory=<directory>` 在本环境尝试把目录当文件打开而失败。
- 一次传递 646 个绝对路径超过 Windows 命令行长度，尚未启动导入程序便失败；失败控制台由会话记录保留，`02_import_explicit.console.log` 保留启动参数。
- 改为 PHIL 中逐行 input.directory 文件名后成功；`import_frames.py` 为成功导入脚本。
- `prepare_processing.py` 从导入对象中排除 `pre_` 预实验并记录六个正式扫描几何信息。

## 原始数据处理与对称性
- 不指定已知晶胞的联合指标化成功（必须显式指定 `joint_indexing=True`）；Bravais 检验选出 oP，伪四方候选的几何残差显著更差。
- 独立精修两个探测器面板，以适应弧形探测器的实际几何；六个扫描有独立、随扫描变化的取向，但晶胞用精确等量变化约束保持相同。
- 最终几何精修使用 2004 个质心；晶胞为 5.7928551、5.9663475、12.2935633 Å，三个角由正交晶系固定为 90°。几何残差约 0.016/0.014 mm、0.066°。
- 三个轴向的奇数反射均支持 2₁ 消光；DIALS 与独立 SHELXT 解均支持 P2₁2₁2₁（No.19）。SHELXT 从数据定位全部六个非氢原子；初始 C3O3 元素指派结合输入式、连接关系及后续氢密度修正为 C3NO2。未导入参考结构。
- 本环境 DIALS 对有精确约束的精修不默认计算晶胞 ESD。`common_cell_covariance.py` 在独立进程内将约束参数协方差用 T V Tᵀ 展开，传播至晶胞；未改安装软件。保存 normal-matrix 逆、约束展开矩阵和协方差。拟合晶胞不变。晶胞长度 ESD 分别 0.0008803、0.0003792、0.0020320 Å；包含相关项的体积为 424.8924(1519) Å³，报告为 424.89(15) Å³。SHELXL 原生忽略一般晶胞相关项时写为 424.89(10)，原始 CIF 另存。

## 束挡/支撑阴影：关键的数据校正
- 未加掩膜时，部分理论允许反射的实测强度接近零；同一等价反射在另一位置有数千计数。其所在位置的原始背景也显著降低，证据不只来自 Fo−Fc。
- 对六个扫描按四种探测器几何累积原始像素，识别持续零计数区域，并为部分透射的边缘加入保守矩形保护区。定义见 `mask_definition.json`；原始数值依据见 `shadow_diagnostic_group_*.npz`、`suspicious_reflections_raw_pixels.csv` 和背景日志。
- 重新积分、对称性分析、尺度化；未根据结构残差逐条 OMIT。仍使用 DIALS 标准积分、partiality 与等价反射离群值过滤；并非声称保留所有原始测量。
- 同一骑乘氢模型，未加掩膜/加掩膜的 R1 分别为 0.0600/0.0301，wR2 为 0.2413/0.0804；差值密度从 +0.82/−0.90 降至 +0.18/−0.19 e/Å³。
- 掩膜前后至 0.77 Å 的 Laue 完整度均为 99.83%；至 0.80 Å 为 100%。最终 signed HKL 包含 4098 条观测，其中用于 0.77 Å 精修的非系统消光观测为 3919 条。未加掩膜结果仅为对照，虽然目录历史命名为 `work/final_data`；最终接受的降阶结果在 `work/masked_data`。
- 物理尺度模型保持 Friedel 区别，含扫描尺度、衰减和低阶球谐吸收项。没有使用面形解析吸收、EXTI 或溶剂校正。绝对透射因子未知。

## 最终精修
- 收敛模型在 `work/refine/07_final`，最终投稿格式输出在 `work/refine/10_publication`：六个非氢原子各向异性，三个 N—H 独立坐标和 Uiso 无约束精修，C—H 骑乘（甲基可旋转）。NH 省略差值图的三个最高峰约为 0.62、0.62、0.51 e/Å³，位于三个 NH 方向；支持 NH3+/COO− 两性离子。
- 0.77 Å 截断出于完整度而非只保留强反射；更高分辨率实测延伸至约 0.708 Å，但完整度明显下降。完整 HKL 保留，同时计算 0.80 Å 和全部实测数据的对照模型。
- 最终 964 条 Friedel 独立反射、68 参数、0 几何 restraints；R1=0.0302 [I>2σ(I)]，全数据 R1=0.0322，wR2=0.0732，S=1.095；最大 shift/su<0.001，差值密度 +0.179/−0.182 e/Å³，无非正定位移椭球。
- SHELXL Rint=0.0365（Friedel 独立合并）；Laue 合并 Rmerge 约 0.038。596 是合并 Friedel 后的独立数，不能与 964 混用。LIST 6 FCF 同样使用 596 个 Fourier 合并反射；未经合并的 HKL/MTZ 保留 Friedel 信息。
- Parsons Flack 0.250(857)，不能确定绝对结构。文件中的手性方向为任意选择，不能据此给出 L/D 或 R/S，也不能解释为精确的反演孪晶比例。

## 兼容性错误与保留记录
- `01_nonH` 的 CELL 行过长、`02_riding_H` 的 HFIX 位置不正确，均使 SHELXL 报错但进程返回 0。保留失败记录；成功替代为 `01b_nonH`/`02b_riding_H`。交付不使用失败输出。
- 初次统计脚本对 sigma_filtering 传入字符串而非 None，及从非反常 ASU 的 MTZ 误算 Friedel 差，已改为读取 signed HKLF4 并重新统计。
- 初次本地验证错误地把 LIST 6 Fourier 合并后的 R1 与 Friedel 独立精修 R1 比较；改为与 SHELXL 的 Fourier 合并 R1 比较。这是验证口径修正，不是结构或强度改动。

## 交付与验证范围
- 结构、支持的实验元数据、最终降阶结果和中文说明写入 `deliverables/`。原始 SHELXL CIF 与元数据修改表独立保留。
- 本地验证脚本检查 CIF、化学式、正定 ADP、独立重算结构因子、分辨率敏感性、原始帧校验和及隔离目录复跑；结果以实际 `validation.json` 为准。
- 最终交付改为 LIST 4 FCF（964 条）；另存原 LIST 6 差值图。初次 PLATON 指出的 LIST 6 格式/统计问题已实际消除，并补 CONF 扭转角表。
- LIST 4 独立复算 R1=0.030218、wR2=0.073239、S=1.095429；独立 Fc 振幅与 FCF 差异 R=0.000691，符合 CIF 舍入精度。隔离目录复跑重现最终指标；646 原始帧哈希全部匹配。两个分辨率对照的非氢最大位移为 0.000690/0.000907 Å。
- 本地 PLATON 写出了带完整汇总的文本报告，但进程未自行退出，在 45 秒被终止。最终为 3 A、0 B、3 C、6 G；A 为缺透射因子/形貌，C 为缺尺寸，G 含绝对结构不确定、体积 ESD 相关项、一个缺失反射等。原报告和逐项回复保留，不能把这称为无警报通过。
- 不冒充官方在线 checkCIF 通过。不联网上传样品。晶体尺寸/颜色、精确完整仪器型号/光学配置、实验者/来源及绝对构型仍未知。
